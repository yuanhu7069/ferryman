[project]
name = "test-data"
version = "0.1.0"
description = "Test data generator for Ferryman CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyarrow>=14",
    "openpyxl>=3",
    "fastavro>=1",
]
